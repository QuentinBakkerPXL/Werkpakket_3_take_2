{% set products = craft.entries.section('product').all() %}


<section>
    <div class="gallery">
        {% for product in products %}
            <div class="product">
                <img src="{{ product.productimage }}" alt={{ product.title }}>
                <h1>{{ product.title }}</h1>
            </div>
        {% endfor %}
    </div>
</section>
