<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Navbar</title>
    {% do craft.app.view.registerCssFile('@web/css/Footer.css') %}
</head>
<body>
<section>
    <footer>
        <div class="footer-content">
            <h3>{{ footer.websitetitle }}</h3>
            <p>{{ footer.websitemail }}</p>
            <p>{{ footer.websitephone }}</p>
            <p class="bottom-text">{{ footer.websitelocation }}</p>
        </div>
    </footer>
</section>
</body>
</html>
